part of magnussuther.chrome_extension_saml_decoder;

class MessageActions {
  final Action addSamlMessage = new Action();
  final Action addAllSamlMessages = new Action();
  final Action clearAllSamlMessages = new Action();
}